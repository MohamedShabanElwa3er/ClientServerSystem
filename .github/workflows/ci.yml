name: Qt6 CI

on:
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    # ✅ Global environment (headless Qt)
    env:
      QT_QPA_PLATFORM: offscreen
      QT_LOGGING_RULES: "qt.qpa.*=false"

    steps:
    # =========================
    # 1️⃣ Checkout repository
    # =========================
    - name: Checkout code
      uses: actions/checkout@v4

    # =========================
    # 2️⃣ Install dependencies
    # =========================
    - name: Install Qt, build tools, and coverage tools
      run: |
        sudo apt update
        sudo apt install -y \
          qt6-base-dev \
          qt6-tools-dev \
          qt6-tools-dev-tools \
          build-essential \
          cmake \
          libgtest-dev \
          lcov \
          gcovr

    # =========================
    # 3️⃣ Build GoogleTest (Ubuntu ships sources only)
    # =========================
    - name: Build GoogleTest
      run: |
        cd /usr/src/gtest
        sudo cmake .
        sudo make -j$(nproc)
        sudo cp lib/*.a /usr/lib

    # =========================
    # 4️⃣ Clean build artifacts
    # =========================
    - name: Clean build artifacts
      run: |
        find . -name Makefile -delete
        find . -name "*.o" -delete
        find . -name "moc_*.cpp" -delete
        rm -f .qmake.stash

    # =========================
    # 5️⃣ Preflight checks (FAIL FAST)
    # =========================
    - name: Verify test sources exist
      run: |
        echo "🔍 Checking Qt tests..."
        for d in tests/qt/test_*; do
          name=$(basename "$d")
          test -f "$d/$name.cpp" || { echo "❌ Missing $d/$name.cpp"; exit 1; }
        done

        echo "🔍 Checking GoogleTest sources..."
        for pro in tests/gtest/*/*.pro; do
          dir=$(dirname "$pro")
          grep -o '[^ ]*\.cpp' "$pro" | while read src; do
            test -f "$dir/$src" || { echo "❌ Missing $dir/$src"; exit 1; }
          done
        done

    # =========================
    # 6️⃣ Configure project (qmake + coverage)
    # =========================
    - name: Configure project (with coverage)
      run: |
        qmake6 -r ClientServerSystem.pro \
          "QMAKE_CXXFLAGS+=--coverage -O0" \
          "QMAKE_LFLAGS+=--coverage"

    # =========================
    # 7️⃣ Build project
    # =========================
    - name: Build project
      run: |
        make -j$(nproc)

    # =========================
    # 8️⃣ Run QtTests (generate .gcda)
    # =========================
    - name: Run QtTests
      run: |
        ./tests/qt/test_parser/test_parser
        ./tests/qt/test_append/test_append
        ./tests/qt/test_info/test_info
        ./tests/qt/test_cpu/test_cpu

    # =========================
    # 9️⃣ Run GoogleTests
    # =========================
    - name: Run GoogleTests
      run: |
        ./tests/gtest/test_commands/test_commands

    # =========================
    # 🔟 Generate coverage report
    # =========================
    - name: Generate coverage report
      run: |
        lcov --capture \
             --directory . \
             --ignore-errors empty \
             --output-file coverage.info

        lcov --remove coverage.info \
             '/usr/*' \
             '*/tests/*' \
             --output-file coverage.info

        genhtml coverage.info --output-directory coverage-report

    # =========================
    # 1️⃣1️⃣ Generate CI report
    # =========================
    - name: Generate CI report
      run: |
        cat << EOF > CI_REPORT.md
        ✅ CI SUCCESS
        Project: ClientServerSystem
        Author: Mohamed Waaer
        Email: mohamed.waer@coretech-innovations.com
        Commit: $GITHUB_SHA
        Date: $(date)
        EOF

    # =========================
    # 1️⃣2️⃣ Create deliverable ZIP
    # =========================
    - name: Create deliverable ZIP
      run: |
        zip -r ClientServerSystem_Deliverable.zip \
          PROJECT_REPORT.md \
          CI_REPORT.md \
          coverage-report

    # =========================
    # 1️⃣3️⃣ Upload artifacts
    # =========================
    - name: Upload deliverable
      uses: actions/upload-artifact@v4
      with:
        name: ClientServerSystem_Deliverable
        path: ClientServerSystem_Deliverable.zip