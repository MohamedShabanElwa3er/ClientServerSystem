name: Qt6 CI

on:
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    # ✅ Global environment (headless Qt)
    env:
      QT_QPA_PLATFORM: offscreen
      QT_LOGGING_RULES: "qt.qpa.*=false"

    steps:
    # =========================
    # 1️⃣ Checkout repository
    # =========================
    - name: Checkout code
      uses: actions/checkout@v4

    # =========================
    # 2️⃣ Install dependencies
    # =========================
    - name: Install Qt, build tools, and coverage tools
      run: |
        sudo apt update
        sudo apt install -y \
          qt6-base-dev \
          qt6-tools-dev \
          qt6-tools-dev-tools \
          build-essential \
          cmake \
          libgtest-dev \
          lcov \
          gcovr \
          jq

    # =========================
    # 3️⃣ Build GoogleTest
    # =========================
    - name: Build GoogleTest
      run: |
        cd /usr/src/gtest
        sudo cmake .
        sudo make -j$(nproc)
        sudo cp lib/*.a /usr/lib

    # =========================
    # 4️⃣ Clean build artifacts
    # =========================
    - name: Clean build artifacts
      run: |
        find . -name Makefile -delete
        find . -name "*.o" -delete
        find . -name "moc_*.cpp" -delete
        rm -f .qmake.stash

    # =========================
    # 5️⃣ Preflight checks
    # =========================
    - name: Verify test sources exist
      run: |
        echo "🔍 Checking Qt tests..."
        for d in tests/qt/test_*; do
          name=$(basename "$d")
          test -f "$d/$name.cpp" || { echo "❌ Missing $d/$name.cpp"; exit 1; }
        done

        echo "🔍 Checking GoogleTest sources..."
        for pro in tests/gtest/*/*.pro; do
          dir=$(dirname "$pro")
          grep -o '[^ ]*\.cpp' "$pro" | while read src; do
            test -f "$dir/$src" || { echo "❌ Missing $dir/$src"; exit 1; }
          done
        done

    # =========================
    # 6️⃣ Configure project (qmake + coverage)
    # =========================
    - name: Configure project (with coverage)
      run: |
        qmake6 -r ClientServerSystem.pro \
          "QMAKE_CXXFLAGS+=--coverage -O0" \
          "QMAKE_LFLAGS+=--coverage"

    # =========================
    # 7️⃣ Build project
    # =========================
    - name: Build project
      run: |
        make -j$(nproc)

    # =========================
    # 8️⃣ Run QtTests
    # =========================
    - name: Run QtTests
      run: |
        ./tests/qt/test_parser/test_parser
        ./tests/qt/test_append/test_append
        ./tests/qt/test_info/test_info
        ./tests/qt/test_cpu/test_cpu

    # =========================
    # 9️⃣ Run GoogleTests
    # =========================
    - name: Run GoogleTests
      run: |
        ./tests/gtest/test_commands/test_commands

    # =========================
    # 🔟 Capture coverage data (lcov)
    # =========================
    - name: Capture coverage data
      run: |
        lcov --capture \
             --directory . \
             --ignore-errors mismatch,empty \
             --rc geninfo_unexecuted_blocks=1 \
             --output-file coverage.info

        lcov --remove coverage.info \
             '/usr/*' \
             '*/tests/*' \
             --output-file coverage.info

    # =========================
    # 1️⃣1️⃣ Generate coverage JSON (FIXED)
    # =========================
    - name: Generate coverage JSON
      run: |
        gcovr \
          --root . \
          --filter '.*\/common\/.*' \
          --filter '.*\/server\/.*' \
          --exclude '.*\/tests\/.*' \
          --json coverage.json

    # =========================
    # 1️⃣2️⃣ Generate unified HTML report with plots
    # =========================
    - name: Generate unified HTML report
      run: |
        LINE_PERCENT=$(jq '.line_coverage.percent // 0' coverage.json)

        cat << EOF > REPORT.html
        <!DOCTYPE html>
        <html lang="en">
        <head>
          <meta charset="UTF-8">
          <title>ClientServerSystem – CI & Coverage Report</title>
          <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
          <style>
            body { font-family: Arial, sans-serif; background:#f9f9f9; margin:40px; }
            h1, h2, h3 { color:#2c3e50; }
            .section { background:#fff; padding:20px; margin-bottom:25px; border-radius:6px; box-shadow:0 0 6px rgba(0,0,0,.1); }
            .grid { display:grid; grid-template-columns:1fr 1fr; gap:30px; }
            canvas { max-width:100%; }
            .footer { color:#777; font-size:.9em; }
          </style>
        </head>

        <body>

        <h1>✅ ClientServerSystem</h1>
        <h2>CI, Testing & Coverage Report</h2>

        <div class="section">
          <h3>👤 Author</h3>
          <p><strong>Name:</strong> Mohamed Waaer</p>
          <p><strong>Email:</strong> mohamed.waer@coretech-innovations.com</p>
        </div>

        <div class="section">
          <h3>📊 Test Coverage Metrics</h3>
          <div class="grid">
            <canvas id="coveragePie"></canvas>
            <canvas id="coverageBar"></canvas>
          </div>
        </div>

        <script>
          new Chart(document.getElementById('coveragePie'), {
            type: 'pie',
            data: {
              labels: ['Covered Code', 'Not Covered'],
              datasets: [{
                data: [$LINE_PERCENT, 100 - $LINE_PERCENT],
                backgroundColor: ['#4DD0E1', '#FF9F40']
              }]
            }
          });

          new Chart(document.getElementById('coverageBar'), {
            type: 'bar',
            data: {
              labels: ['QtTest', 'GoogleTest'],
              datasets: [{
                label: 'Coverage (%)',
                data: [$LINE_PERCENT, $LINE_PERCENT],
                backgroundColor: ['#4DD0E1', '#3A4A62']
              }]
            },
            options: { scales: { y: { beginAtZero: true, max: 100 } } }
          });
        </script>

        <div class="footer">
          Generated automatically by GitHub Actions CI
        </div>

        </body>
        </html>
        EOF

    # =========================
    # 1️⃣3️⃣ Create deliverable ZIP
    # =========================
    - name: Create deliverable ZIP
      run: |
        zip -r ClientServerSystem_Deliverable.zip REPORT.html

    # =========================
    # 1️⃣4️⃣ Upload artifacts
    # =========================
    - name: Upload deliverable
      uses: actions/upload-artifact@v4
      with:
        name: ClientServerSystem_Deliverable
        path: ClientServerSystem_Deliverable.zip
