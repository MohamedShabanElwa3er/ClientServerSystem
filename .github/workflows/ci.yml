name: Qt6 CI

on:
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    # ✅ Global environment (headless Qt)
    env:
      QT_QPA_PLATFORM: offscreen
      QT_LOGGING_RULES: "qt.qpa.*=false"

    steps:
    # =========================
    # 1️⃣ Checkout repository
    # =========================
    - name: Checkout code
      uses: actions/checkout@v4

    # =========================
    # 2️⃣ Install dependencies
    # =========================
    - name: Install Qt, build tools, and coverage tools
      run: |
        sudo apt update
        sudo apt install -y \
          qt6-base-dev \
          qt6-tools-dev \
          qt6-tools-dev-tools \
          build-essential \
          cmake \
          libgtest-dev \
          lcov \
          gcovr

    # =========================
    # 3️⃣ Build GoogleTest
    # =========================
    - name: Build GoogleTest
      run: |
        cd /usr/src/gtest
        sudo cmake .
        sudo make -j$(nproc)
        sudo cp lib/*.a /usr/lib

    # =========================
    # 4️⃣ Clean build artifacts
    # =========================
    - name: Clean build artifacts
      run: |
        find . -name Makefile -delete
        find . -name "*.o" -delete
        find . -name "moc_*.cpp" -delete
        rm -f .qmake.stash

    # =========================
    # 5️⃣ Preflight checks
    # =========================
    - name: Verify test sources exist
      run: |
        echo "🔍 Checking Qt tests..."
        for d in tests/qt/test_*; do
          name=$(basename "$d")
          test -f "$d/$name.cpp" || { echo "❌ Missing $d/$name.cpp"; exit 1; }
        done

        echo "🔍 Checking GoogleTest sources..."
        for pro in tests/gtest/*/*.pro; do
          dir=$(dirname "$pro")
          grep -o '[^ ]*\.cpp' "$pro" | while read src; do
            test -f "$dir/$src" || { echo "❌ Missing $dir/$src"; exit 1; }
          done
        done

    # =========================
    # 6️⃣ Configure project (qmake + coverage)
    # =========================
    - name: Configure project (with coverage)
      run: |
        qmake6 -r ClientServerSystem.pro \
          "QMAKE_CXXFLAGS+=--coverage -O0" \
          "QMAKE_LFLAGS+=--coverage"

    # =========================
    # 7️⃣ Build project
    # =========================
    - name: Build project
      run: |
        make -j$(nproc)

    # =========================
    # 8️⃣ Run QtTests
    # =========================
    - name: Run QtTests
      run: |
        ./tests/qt/test_parser/test_parser
        ./tests/qt/test_append/test_append
        ./tests/qt/test_info/test_info
        ./tests/qt/test_cpu/test_cpu

    # =========================
    # 9️⃣ Run GoogleTests
    # =========================
    - name: Run GoogleTests
      run: |
        ./tests/gtest/test_commands/test_commands

    # =========================
    # 🔟 Generate coverage report
    # =========================
    - name: Generate coverage report
      run: |
        lcov --capture \
             --directory . \
             --ignore-errors mismatch,empty \
             --rc geninfo_unexecuted_blocks=1 \
             --output-file coverage.info

        lcov --remove coverage.info \
             '/usr/*' \
             '*/tests/*' \
             --output-file coverage.info

        genhtml coverage.info --output-directory coverage-report

    # =========================
    # 1️⃣1️⃣ Generate HTML project report
    # =========================

    - name: Generate HTML project report
      run: |
        cat << EOF > PROJECT_REPORT.html
        <!DOCTYPE html>
        <html lang="en">
        <head>
          <meta charset="UTF-8">
          <title>ClientServerSystem – CI & Coverage Report</title>
          <style>
            body { font-family: Arial, sans-serif; margin: 40px; background:#f9f9f9; }
            h1, h2 { color:#2c3e50; }
            .section { background:#fff; padding:20px; margin-bottom:20px; border-radius:6px; box-shadow:0 0 5px rgba(0,0,0,.1); }
            table { width:100%; border-collapse:collapse; }
            th, td { border:1px solid #ccc; padding:10px; }
            .success { color:green; font-weight:bold; }
            iframe { width:100%; height:900px; border:1px solid #ccc; }
            .footer { font-size:.9em; color:#777; }
          </style>
        </head>
        <body>
    
        <h1>✅ ClientServerSystem</h1>
        <h2>CI, Testing & Coverage Report</h2>
    
        <div class="section">
          <h3>👤 Author</h3>
          <p><strong>Name:</strong> Mohamed Waaer</p>
          <p><strong>Email:</strong> mohamed.waer@coretech-innovations.com</p>
        </div>
    
        <div class="section">
          <h3>📌 Project Overview</h3>
          <p>Qt 6–based C++ client–server system with automated CI, testing, and coverage.</p>
        </div>
    
        <div class="section">
          <h3>🔁 CI Summary</h3>
          <table>
            <tr><th>Platform</th><td>GitHub Actions</td></tr>
            <tr><th>OS</th><td>Ubuntu</td></tr>
            <tr><th>Commit</th><td>$GITHUB_SHA</td></tr>
            <tr><th>Date</th><td>$(date)</td></tr>
          </table>
        </div>
    
        <div class="section">
          <h3>🧪 Test Results</h3>
          <ul>
            <li class="success">Qt Tests – PASSED</li>
            <li class="success">GoogleTests – PASSED</li>
          </ul>
        </div>
    
        <div class="section">
          <h3>📊 Test Coverage</h3>
          <p>
            The interactive coverage report is embedded below.
            If it does not render, open
            <code>coverage-report/index.html</code> directly.
          </p>
          <iframe src="./coverage-report/index.html"></iframe>
        </div>
    
        <div class="footer">
          Generated automatically by GitHub Actions CI
        </div>
    
        </body>
        </html>
        EOF

    # =========================
    # 1️⃣2️⃣ Create deliverable ZIP
    # =========================
    - name: Create deliverable ZIP
      run: |
        zip -r ClientServerSystem_Deliverable.zip \
          PROJECT_REPORT.html \
          CI_REPORT.md \
          coverage-report

    # =========================
    # 1️⃣3️⃣ Upload artifacts
    # =========================
    - name: Upload deliverable
      uses: actions/upload-artifact@v4
      with:
        name: ClientServerSystem_Deliverable
        path: ClientServerSystem_Deliverable.zip